from DurrandHoyer import DH
import numpy as np
table = np.array([1,2,3,4])

test = DH(table)

output = test.algorithm()
    if table(output) == min(table):
        return True
    else
        return False